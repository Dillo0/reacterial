var React = require('react');

module.exports = React.createClass({

    getInitialState(){
        // Los return siempre regresan un objeto.
        return {};
    },

    render() {
        return (
            <div>VUTD First React Component</div>
        );
    }

});
